{
  "name": "realspec",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "examples/minimart-go-nuxt/frontend",
    "examples/minimart-java-next/frontend"
  ],
  "scripts": {
    "build": "npm run build -w @realspec/cli",
    "test": "npm run test -w @realspec/cli && node --test scripts/check-spec-parity.test.mjs"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.0.5",
    "@types/node": "^22.13.1"
  }
}
